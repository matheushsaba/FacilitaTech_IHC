<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.FacilitaTech"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SelectUserActivity"/>
        <activity android:name=".HelperHomeActivity"/>
        <activity android:name=".HelpSeekerHomeActivity"/>
        <activity android:name=".RecordedHelpsActivity"/>
        <activity android:name=".VideoPlayerActivity"/>
        <activity android:name=".ConfirmRecordingActivity"/>
        <activity android:name=".NameRecordingActivity"/>
        <activity android:name=".TouchValueActivity"/>
        <activity android:name=".SharingScreen" />
        <activity android:name=".WaitPageActivity" />
    </application>

</manifest>